
fun main(args: Array<String>) {

    var sum = 0
    var i = 10

    while (i != 0) {
        sum += i     // sum = sum + i;
        --i
    }
    println("sum = $sum")
}



fun main(args: Array<String>) {

    var sum = 0
    var i = 10

    while (i != 0) {
        sum += 2     // sum = sum + i;
        --i
    }
    println("sum = $sum")
}